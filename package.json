{
  "name": "ratelimiter-ts",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "rate-limit",
    "ratelimiter",
    "typescript",
    "express",
    "fixed-window",
    "sliding-window"
  ],
  "author": "",
  "license": "ISC",
  "description": "Lightweight TypeScript building blocks for implementing fixed-window and sliding-window rate limiting with pluggable storage backends.",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
